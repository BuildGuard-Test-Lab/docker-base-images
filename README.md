# Docker Base Images

Custom base Docker images with security hardening and standard tooling.

## Images
- `base-node` — Node.js base with security patches
- `base-python` — Python base with common ML dependencies
- `base-go` — Go build environment
